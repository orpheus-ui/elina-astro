deployment:
  tasks:
    - install_dependencies: npm install
    - build_project: npm run build
    - deploy_files:
        command: |
          rm -rf ~/public_html/*
          cp -r dist/* ~/public_html
