deployment:
  tasks:
    - install_dependencies:
        command: /opt/cpanel/ea-nodejs16/bin/npm install
        cwd: .
    - build_project:
        command: /opt/cpanel/ea-nodejs16/bin/npm run build
        cwd: .
    - deploy_files:
        command: cp -r dist/* ~/public_html
        cwd: .
